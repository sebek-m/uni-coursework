# A program that allows for replicating Seibel's 1023 alternatives experiment from 1963

## How to run:
### When in the main directory:
```
npm install
npm run build
```
### When in the 'server' directory:
```
pip install -r requirements.txt
./start.sh
```